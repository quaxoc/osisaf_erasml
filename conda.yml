name: erasml_torch

channels:
  - defaults
  - pytorch
  - nvidia
  - conda-forge
  
dependencies:
  - python=3.11
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::torchaudio
  - pytorch::pytorch-cuda=12.6
  - conda-forge::pygrib
  - conda-forge::netCDF4
  - conda-forge::cartopy
  - matplotlib
  - scipy
  - plotly
  - scikit-learn
  - pandas
  - numpy
  - pyproj
  - notebook
  - pip
  - pip:
    - tqdm
    # wandb is only needed for training (experiment tracking), not for inference
    # - wandb